version: "3.8"
services: 
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - 8080:8080
 #   depends_on:
  #     -db

  frontend:
    build: 
      context: ./frontend 
      dockerfile: Dockerfile
    ports:
      - 80:80 
    depends_on:
       - backend
      
